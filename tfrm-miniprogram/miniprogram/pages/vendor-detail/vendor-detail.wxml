<view class="vendor-detail-container" wx:if="{{vendor}}">
  <view class="header-section">
    <view class="vendor-logo-large">
      <text class="logo-text">{{vendor.name.substring(0, 2)}}</text>
    </view>
    <view class="vendor-header-info">
      <text class="vendor-name">{{vendor.name}}</text>
      <view class="vendor-status {{vendor.status === 'Active' ? 'active' : 'inactive'}}">
        {{vendor.status === 'Active' ? '合作中' : '暂停'}}
      </view>
      <view class="vendor-rating" wx:if="{{vendor.rating}}">
        <text class="rating-icon">⭐</text>
        <text class="rating-value">{{vendor.rating}}</text>
      </view>
    </view>
  </view>

  <view class="detail-section">
    <view class="section-title">联系信息</view>
    <view class="info-grid">
      <view class="info-item">
        <text class="info-label">联系人</text>
        <text class="info-value">{{vendor.contact}}</text>
      </view>
      <view class="info-item">
        <text class="info-label">电话</text>
        <view class="info-value-row">
          <text class="info-value">{{vendor.phone}}</text>
          <view class="copy-btn" bindtap="handleCopyPhone">
            <text class="copy-icon">📋</text>
          </view>
        </view>
      </view>
      <view class="info-item">
        <text class="info-label">邮箱</text>
        <text class="info-value">{{vendor.email}}</text>
      </view>
    </view>
  </view>

  <view class="detail-section" wx:if="{{vendor.address}}">
    <view class="section-title">地址信息</view>
    <view class="address-box">
      <text class="address-icon">📍</text>
      <text class="address-text">{{vendor.address}}</text>
    </view>
  </view>

  <view class="detail-section">
    <view class="section-title">业务范畴</view>
    <view class="categories-list">
      <view 
        wx:for="{{vendor.category}}" 
        wx:key="*this" 
        class="category-item"
      >
        <text class="category-text">{{item}}</text>
      </view>
    </view>
  </view>

  <view class="detail-section" wx:if="{{vendor.specialties && vendor.specialties.length > 0}}">
    <view class="section-title">核心专长</view>
    <view class="specialties-list">
      <text 
        wx:for="{{vendor.specialties}}" 
        wx:key="*this" 
        class="specialty-tag"
      >
        {{item}}
      </text>
    </view>
  </view>

  <view class="detail-section">
    <view class="section-title">合作备注</view>
    <textarea 
      class="notes-textarea"
      placeholder="在此记录合作备注、历史记录等..."
      value="{{notes}}"
      bindinput="onNotesInput"
      auto-height
      maxlength="-1"
    />
    <button class="btn-save-notes" bindtap="handleSaveNotes">
      保存备注
    </button>
  </view>

  <view class="action-section">
    <button class="btn-edit" bindtap="handleEdit">编辑供应商</button>
    <button class="btn-delete" bindtap="handleDelete">删除供应商</button>
  </view>
</view>

<view wx:else class="loading-container">
  <view class="loading-spinner"></view>
  <text class="loading-text">加载中...</text>
</view>
